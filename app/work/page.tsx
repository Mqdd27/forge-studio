"use client"; import { useState } from "react"; import { PageIntro } from "../../components/page-intro"; import { cases } from "../../data/site"; import { WorkCard } from "../../components/work-card";
const filters=["All","Custom App","SaaS","Automation","Integration"];
export default function Work(){const [filter,setFilter]=useState("All");const shown=filter==="All"?cases:cases.filter(x=>x.category===filter);return <main><PageIntro eyebrow="Selected work" title="Built around the problem, not the technology."><p>A collection of engineering projects demonstrating our approach to complex problem-solving and business-first software architecture.</p></PageIntro><div className="container work-main"><div className="filters">{filters.map(x=><button className={filter===x?"active":""} key={x} onClick={()=>setFilter(x)}>{x}</button>)}</div>{shown.length?<div className="grid-3">{shown.map((x,i)=><WorkCard item={x} index={cases.indexOf(x)} key={x.title}/>)}</div>:<div className="empty">No projects in this category yet. <a href="/contact">Talk to us about your challenge →</a></div>}</div><style jsx>{`.work-main{padding-bottom:112px}.filters{display:flex;gap:28px;border-bottom:1px solid var(--border);margin-bottom:48px;overflow-x:auto}.filters button{background:none;border:0;border-bottom:2px solid transparent;padding:0 0 16px;color:var(--grey);white-space:nowrap;cursor:pointer}.filters button.active{color:var(--accent);border-color:var(--accent);font-weight:600}.empty{border:1px dashed var(--border);padding:64px;text-align:center;color:var(--grey)}.empty a{color:var(--accent);display:block;margin-top:12px}@media(max-width:639px){.work-main{padding-bottom:72px}.filters{gap:20px;margin-bottom:32px}}`}</style></main>}
