import type { products } from "../data/site";
export function ProductCard({ item }: { item: (typeof products)[number] }) { const variant = item.status === "Beta" ? "beta" : item.status === "Live" ? "live" : "soon"; return <article className="card product-card"><div className="product-top"><div className="product-mark">FS</div><span className={`status status-${variant}`}>{item.status}</span></div><h3>{item.name}</h3><p>{item.tagline}</p><span className="tag">{item.category}</span><a className="text-link" href="#">{item.status === "Coming Soon" ? "Notify me" : "Visit product →"}</a></article>; }
