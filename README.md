# Forge Studio

Company profile website for Forge Studio, built with Next.js App Router and the Stitch design system.

## Run locally

```bash
npm install
npm run dev
```

Routes: `/`, `/services`, `/work`, `/products`, `/about`, `/insights`, and `/contact`.
